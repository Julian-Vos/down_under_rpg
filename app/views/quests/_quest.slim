tr data-category=quest.category
  td.category-color class=quest.category
  td
    = quest.description
  td
    = quest.experience
    = ' XP'
  td
    - if quest.completed
      = link_to seal_quests_path do
        = '✓'
    - elsif @current_user
      = link_to 'complete', complete_quest_path(quest), method: :patch
